# Details for our datasource
spring.datasource.url=jdbc:postgresql://db.doc.ic.ac.uk/lb1317?ssl=true&sslfactory=org.postgresql.ssl.NonValidatingFactory
spring.datasource.username=lb1317
spring.datasource.password=ZfJUKV7QGV

# Hibernate properties
spring.jpa.database-platform = org.hibernate.dialect.PostgreSQL94Dialect
spring.jpa.show-sql = true

# validate = load existhing database
# create = make a new one (DROPS EVERYTHING WATCH OUT)
spring.jpa.hibernate.ddl-auto = validate
spring.jpa.hibernate.naming.implicit-strategy = org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl
spring.jpa.properties.hibernate.format_sql=true

# debugging
debug=true

# DAO (PersistenceExceptionTranslationAutoConfiguration)
spring.dao.exceptiontranslation.enabled=true